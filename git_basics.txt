git_basics

git log
#git log will show: head, active branch, inactive branch, past commits, will display how many commits behind the remote is

#creating tags

git tag -a v1.0 -m "message"

git tag -d <tagname>
#delete tag

git branch
#show local branches

git branch -a
#show all local and remote branches

git branch -r
#show remote branches

git checkout -b (branch_name)
#create a new branch

git checkout (branch_name)

git remote
#show connected remote repo

git remote -v

git pull
#list branches from remote

git push -u origin head
git push -u -v origin (branch_name)

